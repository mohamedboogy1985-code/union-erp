# دليل المساهمة — Union ERP

شكراً لاهتمامك بالمساهمة! يرجى اتباع الخطوات التالية.

## كيف تساهم

1. اعمل Fork للمستودع ثم أنشئ فرعاً وصفياً:
   ```bash
   git checkout -b feature/اسم-الميزة
   ```
2. نفّذ التعديل مع الالتزام بمعايير الكود أدناه.
3. شغّل الاختبارات والتأكد من نجاحها:
   ```bash
   npm test
   ```
4. أرسل Pull Request مع وصف واضح لما تغيّر ولماذا.

## معايير الكود

- **TypeScript** في كل الملفات الجديدة (لا JS خام).
- الرسائل الموجهة للمستخدم **بالعربية** داخل منطق الأعمال.
- كل خدمة جديدة في `server/services/` يجب أن:
  - تعمل بدون اعتماديات خارجية (fallback رشيق)،
  - تكون قابلة للاختبار عبر `test/improvements.test.ts` بنمط `assert`.
- أي نقطة API جديدة:
  - تُوثق في `docs/API.md`،
  - لا تكسر شكل الاستجابات القديمة.
- العمليات المالية تمر حتماً عبر `erpStore.recordAudit(...)`.

## أنواع المساهمات المرحب بها

| النوع | أمثلة |
|-------|-------|
| ميزات خطة التحسين | راجع `docs/IMPROVEMENTS.md` للبنود غير المنفذة |
| إصلاح أخطاء | تقرير مشكلة + خطوات إعادة إنتاج |
| توثيق | تحسين `docs/*` والترجمة |
| اختبارات | حالات حدية للمحرك المحاسبي والصوت وOCR |

## الإبلاغ عن مشكلة

افتح Issue يتضمن: الوصف، خطوات إعادة الإنتاج، السلوك المتوقع والفعلي، وبيئة التشغيل (نظام/إصدار Node).

## الترخيص

بالمساهمة أنت توافق على نشر مساهمتك تحت ترخيص MIT للمشروع.
