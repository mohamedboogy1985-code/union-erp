services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: union
      POSTGRES_PASSWORD: union
      POSTGRES_DB: union_erp
    ports: ["5432:5432"]
    volumes: [postgres_data:/var/lib/postgresql/data]
volumes:
  postgres_data:
